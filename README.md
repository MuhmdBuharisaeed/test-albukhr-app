# test-albukhr-app

ALBUKHR Testnet App
Testnet-only frontend baseline for test.albukhr.com.
Supabase: Test project only
Network: testnet
Public registry: get_public_project_registry(p_network='testnet')
Mainnet investment data is not queried
Staking, withdrawals and privileged user RPCs remain locked until the secure Testnet authentication gateway is implemented.
